# Creating PRs

Use this rule when creating PRs.

- Always make sure the default branch is up-to-date before creating a PR.
- Use `gh pr create --fill` to create a PR and fill in the details (DO NOT write your own PR description)